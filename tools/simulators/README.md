# Vendor API simulators

WireMock / stub servers for Okta and SailPoint ISC contract tests.

MVP direction:

- `okta/` — Users and Groups API stubs
- `sailpoint-isc/` — IdentityNow / ISC API stubs

Simulators ship before live vendor credentials are required (ADR capability matrix).
